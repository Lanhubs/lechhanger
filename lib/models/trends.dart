import 'dart:ffi';

class updates {
  String? tokenImg;
  String? tokenName;
  String? tokenProgress;
  Float? percentage;

  updates({this.tokenName, this.tokenProgress, this.tokenImg, this.percentage});
}
